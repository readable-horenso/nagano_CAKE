<h2>
  search for "<%= @word %>"
</h2>

<table class="table table-hover table-inverse">
    <thead>
      <tr>
        <th></th>
        <th>Name</th>
      </tr>
    </thead>
    <tbody>
      <% @records.each do |item| %>
        <tr>
          <td></td>
          <td><%= item.name %></td>
        </tr>
      <% end %>
    </tbody>
</table>