<!--ヘッダーのパーシャル-->

<h1>商品詳細</h1>

<div>
  <%= image_tag @item.image, size:'150x150' %>
  <p>商品名 <%= @item.name %></p>
  <p>商品説明 <%= @item.description %></p>
  <p>ジャンル <%= @item.genre.name %></p>
  <p>税込価格<br>
  (税抜価格) <%= @item.with_tax_price.to_s(:delimited) %>(<%= @item.price.to_s(:delimited) %>) 円</p>
  <p>販売ステータス <% if @item.is_valid %>
                    <%= '販売中' %>
                    <% else %>
                    <%= '販売停止中' %>
                    <% end %></p>
</div>

<%= link_to '編集する', edit_admin_item_path(@item), class: 'btn btn-success item_edit' %>

<!--フッターのパーシャル-->