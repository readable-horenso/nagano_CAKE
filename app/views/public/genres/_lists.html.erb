<table class="table table-hover">
  <thead>
    <tr>
      <th>ジャンル一覧</th>
    </tr>
  </thead>
  <% genres.each do |genre| %>
  <tr>
    <td>
      <%= link_to genre_path(genre), class: "text-dark" do  %>
        <%= genre.name %>
      <% end %>
    </td>
  </tr>
  <% end %>
</table>
